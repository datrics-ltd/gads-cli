# gads-cli Progress

## Completed

### 2026-03-18 — Phase 1.1 Project Scaffolding
- Initialized Go module `github.com/datrics-ltd/gads-cli` (Go 1.24.1)
- Created directory structure: `cmd/`, `internal/api/`, `internal/auth/`, `internal/config/`, `internal/output/`
- `main.go` — entry point with `var version = "dev"` injectable via `-ldflags`
- `cmd/root.go` — Cobra root command with global flags: `--customer-id/-c`, `--output/-o`, `--profile/-p`, `--verbose/-v`, `--quiet/-q`, `--no-color`; Viper binding with `GADS_` env prefix
- `cmd/version.go` — `gads version` subcommand
- `internal/config/config.go` — Viper config loading `~/.gads/config.yaml`, auto-creates dir, binds all GADS_ env vars
- `internal/api/client.go` — placeholder Client struct
- `internal/auth/oauth.go` — placeholder TokenSource interface and Credentials struct
- `internal/output/formatter.go` — Formatter interface, Format type, ParseFormat
- `.gitignore` — standard Go gitignore

### 2026-03-18 — Phase 1.2 Configuration
- `cmd/config_cmd.go` — `gads config set|get|list|path` subcommands
  - `set`: reads existing YAML, updates key, writes back with 0600 perms
  - `get`: reads effective value via viper (flag > env > file > default)
  - `list`: reads config file + active GADS_ env vars, redacts sensitive keys (token/secret/password)
  - `path`: prints `~/.gads/config.yaml` path
- Auto-create `~/.gads/` on first use (already in config.Init)
- Env var precedence (GADS_ prefix) already wired in Phase 1.1; this phase validates it works

### 2026-03-18 — Phase 1.3 Authentication — OAuth2
- `internal/auth/credentials.go` — NEW: Credentials struct, Load/Save/Delete, CredentialsPath(); writes to `~/.gads/credentials.json` with 0600 perms
- `internal/auth/oauth.go` — REWRITTEN: full OAuth2 flow
  - `TokenSource` interface (preserved)
  - `OAuthTokenSource` — implements TokenSource, auto-refreshes on expiry via stored refresh token
  - `Login()` — starts local HTTP server on random port, opens browser via `github.com/pkg/browser`, waits for callback with CSRF state check, exchanges code for tokens, persists to credentials file
  - `Logout()` — best-effort token revocation at Google, deletes local credentials
  - `ForceRefresh()` — refreshes access token immediately using stored refresh token
  - `GetStatus()` — returns AuthStatus (logged in, token expiry, dev token / client_id configured)
- `cmd/auth.go` — NEW: `gads auth login|status|logout|refresh` subcommands
  - `login`: calls auth.Login(), prints expiry and credentials path
  - `status`: shows developer token, client_id, and login state; detects GADS_ACCESS_TOKEN env var
  - `logout`: calls auth.Logout(), confirms deletion
  - `refresh`: calls auth.ForceRefresh(), shows confirmation (debug token preview with --verbose)
- Dependencies added: `golang.org/x/oauth2`, `golang.org/x/oauth2/google`, `github.com/pkg/browser`

### 2026-03-18 — Phase 1.4 API Client
- `internal/api/client.go` — REWRITTEN: full HTTP client implementation
  - `Client` struct with `TokenSource` interface, developer token, customer ID, retries, verbose
  - `NewClient(Config)` — config struct pattern for clean construction
  - `Do(*http.Request)` — executes request with body replay on retry, auth header injection, verbose logging
  - `Get(path, headers)` / `Post(path, body, headers)` — convenience methods
  - `injectAuthHeaders()` — injects `developer-token` + `Authorization: Bearer` headers; auto-refreshes via TokenSource
  - Retry loop with exponential backoff (1s, 2s, 4s) for 429/500/503 responses
  - Verbose mode logs full request (method, URL, headers with redaction, body) and response (status, timing) to stderr
- `internal/api/errors.go` — NEW: API error parsing and mapping
  - `ParseAPIError(*http.Response)` — parses Google Ads REST error envelope into `APIError`
  - Maps known error types (AUTHENTICATION_ERROR, AUTHORIZATION_ERROR, QUOTA_ERROR, QUERY_ERROR, REQUEST_ERROR) to human-readable messages
  - Falls back to HTTP status mapping for unstructured errors
- `cmd/root.go` — added `--retries` flag (default 3), bound to viper key `retries`

### 2026-03-18 — Phase 1.5 Output Formatters
- `internal/output/formatter.go` — UPDATED: added `Options`, `Meta`, `New()` factory; `New()` dispatches to the right formatter based on format type and options
- `internal/output/table.go` — NEW: `TableFormatter` using `olekukonko/tablewriter` v1.1.4
  - Borderless layout with header separator line
  - Color coding: ENABLED=green, PAUSED=yellow, REMOVED=red via `fatih/color` (respects `--no-color` and `NO_COLOR` env var)
  - Number formatting with comma separators (1,234 / 1,234.56)
  - Footer row with column totals for numeric data when >1 row present (excludes ID columns)
  - Terminal width detection via `golang.org/x/term` with `COLUMNS` env var fallback
- `internal/output/json.go` — NEW: `JSONFormatter`
  - Pretty-printed by default; `--compact` for single-line
  - Verbose mode wraps output in `{"meta": {...}, "data": [...]}` envelope
- `internal/output/csv.go` — NEW: `CSVFormatter`
  - RFC 4180 compliant via stdlib `encoding/csv`
  - Optional UTF-8 BOM via `--bom` flag for Excel compatibility
  - Raw numbers (no comma formatting or currency symbols)
- `internal/output/formatter_test.go` — NEW: 8 unit tests covering all formatters
- `cmd/root.go` — added `--compact` and `--bom` persistent flags, bound to viper
- Dependencies added: `olekukonko/tablewriter`, `fatih/color`, `golang.org/x/term`

### 2026-03-18 — Phase 1.6 GAQL Queries (Tier 2)
- `internal/api/gaql.go` — NEW: `ExecuteGAQL(customerID, query)` method on `Client`
  - POSTs to `/v18/customers/{customerID}/googleAds:searchStream`
  - Parses streaming JSON array response (`[]SearchGoogleAdsStreamResponse`)
  - Falls back to single-object parse for defensive compatibility
  - Flattens nested result objects to dot-separated key maps (e.g. `campaign.name`)
  - Derives ordered headers from `fieldMask` in response; falls back to first-row keys
  - Normalises customer IDs (strips dashes before use in URL path)
- `cmd/helpers.go` — NEW: `buildAPIClient()` shared helper + `staticTokenSource` for `GADS_ACCESS_TOKEN` env var
- `cmd/query.go` — NEW: `gads query` command
  - `gads query "<GAQL string>"` — inline query
  - `gads query -f <file.gaql>` — query from file
  - Routes results through output formatters (`--output table|json|csv`)
  - `--customer-id` flag (global) overrides default; GAQL syntax errors pass through from API

### 2026-03-18 — Phase 1.7 Raw API Escape Hatch (Tier 3)
- `cmd/api.go` — NEW: `gads api GET|POST <path>` command
  - `GET <path>`: authenticated GET request to Google Ads API
  - `POST <path> -d '<json>'`: POST with inline JSON body
  - `POST <path> -d @<file.json>`: POST with body read from file
  - `POST <path>`: reads body from stdin when no `-d` flag
  - Auto-replaces `{customer_id}` with configured customer ID (dashes stripped)
  - Auto-prepends `https://googleads.googleapis.com` when path starts with `/`
  - Pretty-prints JSON response by default; `--raw` flag for unformatted output
  - `--dry-run` flag shows full request (URL, headers with redaction, body) without sending
  - `-H "Key: Value"` flag for custom headers (repeatable)
  - Routes response through output formatters when `--output json|csv` explicitly set

### 2026-03-18 — Phase 1.8 Integration Testing
- `internal/api/client_test.go` — NEW: 8 tests covering header injection (developer-token + Authorization), retry logic (429/500 → retry, 400 → no retry), POST body replay on retry, URL resolution, header redaction, and error mapping via mock httptest servers
- `internal/api/errors_test.go` — NEW: 9 tests for ParseAPIError covering all known error types (queryError, authenticationError, authorizationError, quotaError, requestError), HTTP status fallback (401/403/429/500), raw body preservation, and extractErrorCode
- `internal/auth/credentials_test.go` — NEW: 6 tests for IsExpired() (expired/valid/empty/30s buffer), SaveCredentials/LoadCredentials round-trip, 0600 file permissions, nil return for missing file, DeleteCredentials idempotency, and directory auto-creation
- `internal/auth/oauth_test.go` — NEW: 7 tests for GetStatus (not logged in, env token, saved creds, expired creds, no dev token), OAuthTokenSource.AccessToken() without network, Refresh() error on missing refresh token; mock OAuth2 callback handler tests (invalid state → 400, error param → 401)
- `internal/config/config_test.go` — NEW: 6 tests for config file read/write, GADS_ env var override, missing file handling, precedence order (Set > env > file), config path resolution, Init() directory auto-creation
- `cmd/api_test.go` — NEW: 11 tests for parseHeaders (single/multiple/empty/invalid/whitespace trim), prettyPrintJSON (valid/invalid JSON), printDryRun (GET/POST with body/custom headers), {customer_id} path substitution, readBody (from flag/file/missing file)
- `internal/output/formatter_test.go` — UPDATED: added TestCSVFormatterEscaping (RFC 4180 commas/quotes/embedded newlines with csv.Reader round-trip), TestTableFormatterColumnAlignment, TestJSONFormatterNumericTypes
- All tests: `go test -short ./...` → all packages green; retry tests pass in full mode

### 2026-03-18 — Phase 2.1 Campaigns
- `cmd/campaigns.go` — NEW: `gads campaigns` command with five subcommands
  - `list`: GAQL query against `campaign` resource; shows ID, name, status, budget (amount_micros), clicks, impressions, cost, CTR; `--status` flag filters by ENABLED/PAUSED/REMOVED
  - `get <campaign-id>`: GAQL lookup by campaign.id; shows same fields plus advertising_channel_type and conversions
  - `pause <campaign-id>`: POST to `campaigns:mutate` with `updateMask=status`, sets status to PAUSED
  - `enable <campaign-id>`: same mutate pattern, sets status to ENABLED
  - `stats <campaign-id>`: GAQL with date filter; `--date-range` for presets (LAST_7_DAYS default), `--from`/`--to` for custom BETWEEN range; shows clicks, impressions, cost, CTR, conversions, conversion_rate, average_cpc
- `cmd/helpers.go` — UPDATED: added shared `renderOutput()` helper (format + options from viper, writes to stdout)
- `cmd/query.go` — UPDATED: refactored to use shared `renderOutput()` helper

### 2026-03-18 — Phase 2.2 Ad Groups
- `cmd/ad_groups.go` — NEW: `gads ad-groups` command with five subcommands
  - `list`: GAQL query against `ad_group` resource; shows ID, name, status, campaign ID, campaign name, clicks, impressions, cost, CTR; `--campaign` flag filters by campaign ID
  - `get <ad-group-id>`: GAQL lookup by ad_group.id; shows same fields plus type and conversions
  - `pause <ad-group-id>`: POST to `adGroups:mutate` with `updateMask=status`, sets status to PAUSED
  - `enable <ad-group-id>`: same mutate pattern, sets status to ENABLED
  - `stats <ad-group-id>`: GAQL with date filter; `--date-range` for presets (LAST_7_DAYS default), `--from`/`--to` for custom BETWEEN range; shows clicks, impressions, cost, CTR, conversions, conversion_rate, average_cpc

## In Progress
(none)

## Notes
- Go installed at `/home/cm/.local/go`
- Build: `/home/cm/.local/go/bin/go build ./...`
